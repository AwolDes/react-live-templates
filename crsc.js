// Stateless React Component
import React, { PropTypes } from "react";

const $COMPONENT$ = (props) => {

  const BlankFunc = (param) => {
  };

  return (
    <div>
      <h1>Stateless component!!</h1>
    </div>
  );
};

$COMPONENT$.propTypes = {
};

$COMPONENT$.defaultProps = {
};

export default $COMPONENT$;
